

const Main = () => {
    return (
        <section
            id="home"
            className="h-screen flex items-center justify-center bg-green-400 bg-cover bg-center"
        ><h2 className="text-5xl font-bold text-red-500">Main</h2>
        </section>
    );
};

export default Main;