const Pricing = () => {
    return (
        <section id="pricing" className="h-screen bg-blue-300 flex items-center justify-center">
            <h2 className="text-5xl font-bold text-orange-700">Pricing</h2>
        </section>
    );
};

export default Pricing;